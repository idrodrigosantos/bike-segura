@model BikeSegura.Models.Imagens

@{
    ViewBag.Title = "Adicionar Imagem";
    Layout = "~/Views/Shared/_Usuario.cshtml";
}

@using (Html.BeginForm("Create", "Imagens", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="container conteudo">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="form-group col-md-8 offset-md-2 col-lg-6 offset-lg-3">
                @Html.LabelFor(model => model.BicicletasId, "Bicicleta", htmlAttributes: new { @class = "control-label" })
                @Html.DropDownList("BicicletasId", (SelectList)ViewBag.MyList, "Selecione Bicicleta", new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.BicicletasId, "", new { @class = "text-danger" })
            </div>
            <div class="form-group col-md-8 offset-md-2 col-lg-6 offset-lg-3">
                @Html.LabelFor(model => model.Imagem, htmlAttributes: new { @class = "control-label" })
                <input type="file" name="arquivoimg" class="form-control-file" multiple />
                @Html.ValidationMessageFor(model => model.Imagem, "", new { @class = "text-danger" })
            </div>
            <div class="form-group col-md-8 offset-md-2 col-lg-6 offset-lg-3">
                <div class="botao-salvar">
                    <a class="btn btn-danger" href="@Url.Action("ListaUsuario", "Imagens")" role="button">
                        <i class="fas fa-times"></i>
                        Cancelar
                    </a>
                    <button type="submit" value="Create" class="btn btn-success">
                        <i class="fas fa-save"></i>
                        Cadastrar
                    </button>
                </div>
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

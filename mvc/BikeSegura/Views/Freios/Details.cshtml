@model BikeSegura.Models.Freio

@{
    ViewBag.Title = "Detalhes Freio";
    Layout = "~/Views/Shared/_Administrador.cshtml";
}

<div class="container conteudo">
    <div class="row">
        <div class="col-sm-6">
            <label><strong>@Html.DisplayNameFor(model => model.Nome)</strong></label>
        </div>
        <div class="col-sm-6">
            <label>@Html.DisplayFor(model => model.Nome)</label>
        </div>
        <div class="form-group col-sm-12">
            <div class="botao-salvar">
                <a class="btn btn-success" href="@Url.Action("Index", "Freios")" role="button">
                    <i class="fas fa-reply"></i>
                    Voltar
                </a>
                <a class="btn btn-primary" href="@Url.Action("Edit", "Freios", new { id = Model.Id })" role="button">
                    <i class="fas fa-pen"></i>
                    Editar
                </a>
            </div>
        </div>
    </div>
</div>

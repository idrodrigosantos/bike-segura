@model BikeSegura.Models.NumerosSeries

@{
    ViewBag.Title = "Excluir Número de Série";
    Layout = "~/Views/Shared/_Usuario.cshtml";
}

<div class="container conteudo">
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Numero)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Tipo)
                    </th>
                    <th>
                        Marca
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Bicicletas.Modelo)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        @Html.DisplayFor(model => model.Numero)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Tipo)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Bicicletas.Marcas.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(model => model.Bicicletas.Modelo)
                    </td>
                    <td>
                        <div class="btn" role="group">
                            @using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()
                                <a class="btn btn-success" href="@Url.Action("ListaNumerosSeries", "NumerosSeries")" role="button">
                                    <i class="fas fa-reply"></i>
                                    Voltar
                                </a>
                                <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalExcluir">
                                    <i class="fas fa-times"></i>
                                    Excluir
                                </button>
                            }
                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<!-- Modal Confirmação Excluir-->
<div class="modal fade" id="modalExcluir" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Excluir Relato</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Fechar">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Deseja realmente excluir o relato sobre o roubo?
            </div>
            <div class="modal-footer">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <button type="button" class="btn btn-success" data-dismiss="modal">Não</button>
                    <button type="submit" value="Delete" class="btn btn-danger">Sim</button>
                }
            </div>
        </div>
    </div>
</div>
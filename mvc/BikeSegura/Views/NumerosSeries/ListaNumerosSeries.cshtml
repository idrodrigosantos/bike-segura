@model IEnumerable<BikeSegura.Models.NumerosSeries>

@{
    ViewBag.Title = "Lista de Números de Séries";
    Layout = "~/Views/Shared/_Usuario.cshtml";
}

<div class="container conteudo">
    <p>
        <a class="btn btn-success" href="@Url.Action("CreateNumeros", "NumerosSeries" )" role="button">
            <i class="fas fa-plus"></i>
            Cadastrar Número de Série
        </a>
    </p>
    <div class="table-responsive">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Numero)
                    </th>
                    <th>
                        Marca
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Bicicletas.Modelo)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Tipo)
                    </th>
                    @*<th>Opções</th>*@
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        @Html.Raw(ViewData["NUMERO"].ToString())
                    </td>
                    <td>
                        @Html.Raw(ViewData["MARCA"].ToString())
                    </td>
                    <td>
                        @Html.Raw(ViewData["MODELO"].ToString())
                    </td>
                    <td>
                        @Html.Raw(ViewData["TIPO"].ToString())
                    </td>
                    @*@foreach (var item in Model)
                    {
                        <td>
                            <div class="btn" role="group">
                                <a class="btn btn-success" href="@Url.Action("Details", new { id = item.Id })" role="button">
                                    <i class="fas fa-list"></i>
                                    Detalhes
                                </a>
                                <a class="btn btn-primary" href="@Url.Action("Edit", new { id = item.Id })" role="button">
                                    <i class="fas fa-pen"></i>
                                    Editar
                                </a>
                                <a class="btn btn-danger" href="@Url.Action("Delete", new { id = item.Id })" role="button">
                                    <i class="fas fa-times"></i>
                                    Excluir
                                </a>
                            </div>
                        </td>
                    }*@
                </tr>
            </tbody>
        </table>
    </div>
</div>
